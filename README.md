## Fetch using custom React Hooks

This repository is to demonstrate data fetching using custom React Hooks while still being typesafe.

Libraries used:

+ Default browser fetch
+ Styled Components
+ Debounce for infinite scroll
+ React Router